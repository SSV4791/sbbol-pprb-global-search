dependencies {
    implementation(project(":search-common:engine-core-api"))
    implementation(project(":search-sink:api"))
}
description =
    "Service ППРБ.Digital.Умный поиск. Серверная часть загрузки документов в полнотекстовый индекс. Реализация sink-api"
